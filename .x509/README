# x509

Repository for x509. [To be used as submodule]

Usage:

`git checkout master`
`git branch <branch>`
`git checkout <branch>`
`cp <credentials/certs> ./`
`git add .`
`git commit`
`git push --set-upstream origin <branch>`
